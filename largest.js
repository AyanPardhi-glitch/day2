//wap to js to check the largest of two numbers
const num1 = 10;
const num2 = 20;
if (num1 > num2) {
    console.log("The largest number is: " + num1);
} else {
    console.log("The largest number is: " + num2);
}